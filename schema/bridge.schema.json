{
  "$comment": [
    "The single source of truth for the native<->web bridge.",
    "",
    "Nothing here is written by hand on either side: tools/gen_bridge.py emits",
    "the C++ request/response structs, the handler interface and the dispatch",
    "table, and the TypeScript types and typed call wrappers. Adding a method",
    "below and rebuilding makes the C++ fail to compile until it is implemented,",
    "which is the point -- the schema cannot drift away from either end.",
    "",
    "Field types: string, int, double, bool, or the name of an entry in",
    "\"types\". Add \"array\": true for a list, \"optional\": true for a field that",
    "may be absent.",
    "",
    "A capability groups methods and events and carries a version. The UI reads",
    "that version from shell.getCapabilities() and degrades when something it",
    "wants is missing or older than it needs. SONORA_DISABLE_CAPS turns one off",
    "at runtime so the degraded path can be exercised on demand instead of only",
    "on an old build. A capability marked \"required\" cannot be turned off: with",
    "shell.getCapabilities() gone there is nothing left to negotiate with."
  ],

  "protocolVersion": 1,

  "types": {
    "Capability": {
      "summary": "One capability, as the shell currently reports it.",
      "fields": [
        { "name": "name", "type": "string", "summary": "Capability name, as used in a method's prefix." },
        { "name": "version", "type": "int", "summary": "Version the shell implements." },
        { "name": "enabled", "type": "bool", "summary": "False when this build or this run has it switched off." }
      ]
    }
  },

  "capabilities": [
    {
      "name": "shell",
      "version": 2,
      "required": true,
      "summary": "Information about the native shell hosting this page.",
      "methods": [
        {
          "name": "getVersion",
          "summary": "Versions of the shell and of the embedded browser.",
          "params": [],
          "result": [
            { "name": "version", "type": "string", "summary": "Sonora's semantic version." },
            { "name": "gitDescribe", "type": "string", "summary": "git describe at build time." },
            { "name": "cefVersion", "type": "string", "summary": "CEF version the shell links." },
            { "name": "chromiumVersion", "type": "string", "summary": "Chromium version behind it." }
          ]
        },
        {
          "name": "echo",
          "summary": "Round-trips a string through the native side. Exists to prove the bridge end to end, and to exercise the failure path: an empty message is rejected.",
          "params": [
            { "name": "message", "type": "string", "summary": "Text to send back." },
            { "name": "repeat", "type": "int", "optional": true, "summary": "How many copies to return. Defaults to 1, maximum 64." }
          ],
          "result": [
            { "name": "message", "type": "string", "summary": "The message, repeated." },
            { "name": "lengthBytes", "type": "int", "summary": "Length of the returned message in UTF-8 bytes." }
          ]
        },
        {
          "name": "getCapabilities",
          "summary": "What this shell exposes right now, including what is switched off. The first call a page should make: everything else depends on the answer.",
          "params": [],
          "result": [
            { "name": "protocolVersion", "type": "int", "summary": "Envelope version the shell speaks." },
            { "name": "capabilities", "type": "Capability", "array": true, "summary": "Every capability the build knows about, enabled or not." }
          ]
        }
      ]
    },
    {
      "name": "diagnostics",
      "version": 1,
      "summary": "Numbers about the running process. Present in every build, and the one capability a deployment might reasonably switch off, which makes it the test subject for negotiation.",
      "methods": [
        {
          "name": "getMetrics",
          "summary": "A snapshot of the bridge's own counters.",
          "params": [],
          "result": [
            { "name": "uptimeMs", "type": "int", "summary": "Milliseconds since the shell finished starting." },
            { "name": "queriesHandled", "type": "int", "summary": "Requests dispatched since startup." },
            { "name": "eventsPosted", "type": "int", "summary": "Events the native side produced." },
            { "name": "eventsDelivered", "type": "int", "summary": "Events that actually reached the page." },
            { "name": "eventsCoalesced", "type": "int", "summary": "Events replaced by a newer one before they were sent. posted = delivered + coalesced + whatever is still pending." }
          ]
        }
      ],
      "events": [
        {
          "name": "heartbeat",
          "coalesce": true,
          "summary": "Emitted far faster than it is delivered, on purpose: it is the shape every later event has -- playback position, buffer level, download progress -- where the producer runs at its own rate and the UI only needs the newest value.",
          "payload": [
            { "name": "sequence", "type": "int", "summary": "Increments once per emission, including the ones that never arrive. Gaps are the coalescing, visible from the page." },
            { "name": "uptimeMs", "type": "int", "summary": "Milliseconds since the shell finished starting." }
          ]
        }
      ]
    }
  ]
}
